const initialState = {};

const Reducer = (state = initialState, action) => {
  const newState = { ...state };

  switch (action.type) {
    default:
      break;
  }

  return newState;
};

export default Reducer;
